 CREATE TABLE commerce(
   commerce_name varchar(50),
   descripcion_of_the_commerce varchar(100),
   email varchar(50),
   phone_number int,
   password varchar(20),
   state varchar(20),
   city varchar(20),
   direction varchar(50),
   commerce_photo_path varchar(50),
   
 );

 CREATE TABLE stock(
   commerce_name varchar(50),
   product_name varchar(50),
   classification varchar(40),
   available_quantity int,
   price float,
   product_photo_path varchar(50)
 );

 
  
 CREATE TABLE employees(
   commerce_name varchar(50),
   name varchar(40),
   last_name varchar(40),
   salary float,
   work_days ,
   working_hours int,
   schedule json[], /*Json object that cotain days and the initial and final time
   of working of every day in the object*/
   pay_day int
 );



 CREATE TYPE banks as ENUMS('Venezuela', 'Banesco', 'Mercantil', 'Provincial, 'Bicentenario');

 CREATE TABLE orders(
   commerce_name varchar(50),
   items json[], /*Each element will containt name of the product, amount, price per item and total price*/
   date_of_purchase date,
   shopping_time time,
   bank banks,
   refence_transaction_number int,
   buyer_data json,
   id_purchase int,
   status bool, /*Confirmado o no confirmado. Se confirmara cuando el personal confirme la transferencia bancaria*/
   /*El cliente podra elegir pagar el 
   payment_by_transfer_or_at_the_stablishment bool,
   delivery bool, /*Esto tendria sentido en caso de que el pago fue hecho via transfencia. Arreglar esto*/ 
 );
